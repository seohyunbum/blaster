# 00 — 오케스트레이터 확정 재정 (설계 정본 인덱스 + 잔여 4건 해소)

> 이 문서가 `docs/design/` 의 **최상위 정본**이다. 01~09 섹션과 아래 재정이 충돌하면 **이 문서가 이긴다**.
> 배경: 2축 fan-out 설계(01~08) → 자유 변형 델타(09) + 결정문 27개 통합 패치 → 최종 정합성 재검. 재검이 병합 전 필수 해소로 지목한 4건(+주요 nit)을 여기서 못박는다.

## 문서 지도 (소유·정본)

| 섹션 | 소유 주제 (정본) |
|---|---|
| 01_concept | 게임 컨셉·코어 루프·**M1 범위 정본**·온보딩 |
| 02_parts | 파츠 카탈로그·스탯 산출(computeStats)·스탯→ShotProfile 매핑(§4.0) |
| 03_visuals | 메시 빌더·toon 팩토리·색칠 구현(머티리얼 캐시) |
| 04_simulation | 탄도·판정·코스 별점·ShotProfile 계약·projectileScale |
| 05_progression | 세이브 루트(SavedGame)·프로필 키·해금·도전과제·코스 행 |
| 06_ux | 레이아웃·입력 스킴·자동저장·undo 스펙·스탯 표시 컴포넌트 |
| 07_tech | 아키텍처·모듈 트리·성능 예산 단일표·마일스톤·verify 체인 |
| 08_safety | 연령 가드레일·팔레트 키·금칙어 단일 정본(§3.1) |
| 09_freeform | **자유 변형(파라메트릭 조형) 전 사양** |

## 잔여 4건 재정 (RESOLVED)

### R1. 발사체 종류(ProjectileKind) = 3종 확정 — 04 를 3종으로 정렬
- **확정**: `dart`(M1 기본·포물선 g≈4·반경 0.025) / `gel`(젤리볼·M2) / `paint`(물감볼·M2+). **거품볼(foam) 폐기** — 재도입 필요 시 08 §4.1 공동 개정 절차로만.
- M1 의 유일 발사체는 `dart`. 04 §2.1·§12 의 4종 표에서 `foam` 행 삭제, `gel`·`paint` 마일스톤을 M2 로.
- 코드 union: `type ProjectileKind = 'dart' | 'gel' | 'paint'`.

### R2. 색칠 = M1 확정 — 07 을 M1 로 정렬
- **확정**: 3존 PaletteKey 채우기 + paint 스테이션은 **M1**(범위 정본 01·03·05·06 다수). 07 의 M3 배치는 오답 → `paint.ts`·paint 스테이션 M1 편입. M3 는 **패턴·스티커·진열장 확장**으로 재정의.

### R3. muzzle 부착 = 배럴 끝 앵커 승계 확정 — 02 를 정렬
- **확정**: `muzzle`(총구 액세서리)은 **바디 소켓이 아니다**. 배럴의 끝 앵커를 승계한다(09 §3.3). 02 §1 몸통별 소켓 표에서 muzzle 열 제거, "배럴 끝 승계" 각주. muzzle 파츠 자체는 M2+.
- M1 소켓 = body 가 제공하는 `barrel`·`sight`(+ 몸통별 차등). SocketId 문자열 = 슬롯명과 동일(`barrel`/`sight`), 03 의 `"mag"` 오기 → 사용 안 함(M1 에 mag 없음).

### R4. PaletteKey = 08 단일 레지스트리 확정 — 03 프리셋은 키 묶음으로
- **확정**: 색 키 정본 = 08 `TOY_PALETTE`(13키, 예 `blasterBlue`·`toyGrayLight`). 03 의 "6프리셋×6색 = 36키"는 **프리셋 = 정본 키의 이름있는 묶음**으로 재정의(신규 키 생성 금지). 저장값·폴백 기본키 모두 실존 13키 중 하나.
- primary 존 = 밝은 색 강제(08 존 차등). 팁(tip)은 주황 고정(가드레일).

## 주요 nit 일괄 재정 (구현 시 이 값을 쓴다)
- 온보딩 시작 파츠 = **팝콘 몸통 + 숏 스냅 배럴**(롱 레일·도트는 ★ 해금). 01 §1.3 정정. (재검 #5)
- 반동: 02 `computeStats` 는 **도(°) 단위** `recoilKickDeg`(0.2~2.5°)를 직접 산출(0..1 미사용). foam 삭제로 탄속 클램프는 dart 20~60 m/s. (재검 #6)
- 간이 별점 ★ 기준 = **명중 수**(04 소유). 01 §1.7 "전멸+시간" 표현 폐기. M1 풍선 마당: ★1=완주, ★2=명중 6+, ★3=명중 9(전 타겟). (재검 #12·#20)
- 홀드 연사 = **M1 이후**(06 정본). 07 §4 "누르면 연사"는 이월 표기. M1 = 클릭/탭 단발. (재검 #8)
- M1 색칠 인터랙션 = 화면 고정 존 버튼 3개(03 §3.3). 06 "3D 존 탭"은 M2. (재검 #10)
- 이름 짓기 모달 = **M2**. M1 자동명(예 "블래스터 1"). (재검 #14)
- verify 체인에 `check:vocab`(금칙어 lint, 08 §3.1) 포함. (재검 #11)
- 코스 모드 타입 = 04 `RangeModeId` 단일. 05 `CourseMode` 폐기, balloon_yard 모드 = `targetLane`. (재검 #9·유령)

## 아들·부모 확인 대기 (열린 질문 — 기본값으로 진행, 나중에 상수 1개로 교체 가능)
1. 게임 표시명: 가칭 **"블래스터 공방"** 으로 진행(아들과 최종 결정 시 UI 라벨만 교체).
2. morph 스탯 델타 체감 강도 — 약하면 ×1.5(04 클램프 안, 수치만).
3. 랜덤 섞기 분포(M1.5) — 기본 균등 [0.1, 0.9].

## 사용자 지시 스코프 변경 로그 (표준 마일스톤보다 우선)
- **2026-07-18 · 다트 팩(탄창)을 M2→현재 빌드로 당김** (사용자 명시 요청 "탄창 시스템, 디자인 때 선택").
  - **결정문 21("M1 무한 탄창·재장전 없음")·04 §1·§3.3 을 부분 대체**: 이제 유한 탄약+재장전을 M1에 구현. 단 **선택제로 후퇴 없음 보장** — 다트 팩 미장착 = 기존과 동일한 무한 탄약(잔탄 "∞"). 기존 저장 총은 전부 다트 팩이 없으므로 M1 경험 무변.
  - **명명 가드레일 준수**: "탄창/탄약"은 08 §3.1 금칙어라 사용자 표기는 **"다트 팩 / 다트"**. `slot` 식별자는 내부적으로 `magazine` 유지(코드 계약), UI 라벨만 토이 표현.
  - capacity/reloadSec 는 04 §3.3 표의 스탯을 그대로 사용(6~30발). 정본 세부 = docs/work-history.md 2026-07-18 다트 팩 항목.

- **2026-07-18 · 검정 본체색 허용 + 미니건 손잡이(몸통 위 그립)** (사용자 명시 요청).
  - **08 §1.2 "primary=밝은 색만" 부분 완화**: 검정(toyBlack)만 본체색 허용(`canBePrimary`). 어두운 회색(toyGrayDark)은 계속 secondary/accent 전용. toyBlack hex 0x2b2f36→0x1e2126.
  - **02 그립 로스터에 grip_minigun 추가**: 몸통 위(gripTop 앵커) 장착 스페이드 핸들. 그립 슬롯은 유지하되 이 파츠만 상단 마운트로 라우팅(assembly). anchors 키에 'gripTop' 특수 마운트 도입.

- **2026-07-19 · 미니건 코어(초대형 구형 몸통) 추가** (사용자 명시 요청 "동그랗게 생기고 엄청나게 큰 미니건 몸통").
  - **02 §3.1 몸통 로스터에 body_minigun 추가**: 전 몸통 중 최대 실루엣의 구형(sphere) 프레임. BODY_DIMS `w/h 0.36·d 0.52`(오브 0.19 대비 폭·높이 ~2배). base P8/R5/A3/W9·weightLimit 18(가장 무겁고 든든, 대신 정확도 낮음). additive 라 기존 저장 총 무영향. 명명: `minigun` 토큰은 금칙어 사전에 없음(기존 grip_minigun·barrelCount 선례와 동일 정책).

- **2026-07-19 · 리볼버(권총류) 부품 추가** (사용자 명시 요청 "리볼버 같은 권총류 부품").
  - **08 §1.1 하드룰 준수 방식**: realistic 권총 몸통(얇고 각진 실루엣)은 가드레일 위반이라 배제. 대신 리볼버의 상징인 **회전 실린더**를 다트 팩으로, **둥근 서부식 그립**을 그립으로 구현(둥글고 통통·다트 끝 노출 = 토이 결).
  - **02 다트 팩 로스터에 mag_revolver 추가**: 리볼버 실린더(축=Z 통통·짧은 실린더 + 앞면 다트 6발 링). capacity 6·reloadSec 1.5. computeStats/세이브/HUD 는 기존 magazine 배선 재사용.
  - **02 그립 로스터에 grip_revolver 추가**: 뒤로 젖힌 캡슐 + 둥근 뒤꿈치. 하단 그립 앵커(일반 그립 경로) 유지.
  - 명명: `revolver`/`리볼버` 는 08 §3.1 금칙어 아님(lint 통과). additive.
